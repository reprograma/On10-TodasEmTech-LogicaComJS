//2. Crie uma função que recebe 2 parâmetros e retorna a multiplicação deles.

function multiplicacao(a, b) {
    return a * b;
}

let res = multiplicacao(prompt('Digite um número'), prompt('Digite outro número'));

console.log(res);