//1. Crie uma função que retorna a palavra (impar/par) de acordo com seu parâmetro.


function name(params) {
    if (params % 2 == 0) {
        return 'Par';
    } else {
        return 'impar';
    }
}