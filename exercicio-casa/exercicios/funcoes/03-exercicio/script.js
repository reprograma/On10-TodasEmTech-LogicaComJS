//3. Crie uma função que calcula 5% de desconto retornando o valor do desconto.

let valor = prompt('Digite um valor para ter o desconto:')

function desconto(valor){
    
    resultado = valor*0.05;
    return resultado
}

desconto(valor)
alert(resultado)