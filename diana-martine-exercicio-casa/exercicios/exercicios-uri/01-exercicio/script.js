// [Exercício 01](https://www.urionlinejudge.com.br/judge/pt/problems/view/1038) 

/* conta();

function conta() {
  let item = parseInt(prompt("Insira aqui o número correspondente ao item"));
  let quantidade = parseInt(
    prompt("Insira aqui a quantidade correspondente ao item")
  );
  let total = "";
  
  if (item == 1) {
    total = 4 * quantidade;
  } else if (item == 2) {
    total = 4.5 * quantidade;
  } else if (item == 3) {
    total = 5 * quantidade;
  } else if (item == 4) {
    total = 2 * quantidade;
  } else if (item == 5) {
    total = 1.5 * quantidade;
  } else if ((item = 0 || item > 5)) {
    alert("Erro, número inválido");
    conta();
  }
  document.writeln(`Total R$: ${total.toFixed(2)}`);
} */