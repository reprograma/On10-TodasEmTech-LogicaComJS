//- [Exercício 02](https://www.urionlinejudge.com.br/judge/pt/problems/view/1134)

/* document.writeln(`MUITO OBRIGADO<br>`);

preferedFuel();

function preferedFuel() {
  let input = parseInt(
    prompt(
      `Selecione o combustível\n1 - Álcool\n2 - Gasolina\n3 - Diesel\n4 - Sair`
    )
  );

  let alcool = 0;
  let gasolina = 0;
  let diesel = 0;

  if (input == 1) {
    let resAlcool = parseInt(
      prompt("Informe a quantidade de clientes que usam álcool")
    );
    alcool = resAlcool;
    document.writeln(`Álcool: ${alcool}<br>`);
    preferedFuel();
  } else if (input == 2) {
    let resGasolina = parseInt(
      prompt("Informe a quantidade de clientes que usam gasolina")
    );
    gasolina = resGasolina;
    document.writeln(`Gasolina: ${gasolina}<br>`);
    preferedFuel();
  } else if (input == 3) {
    let resDiesel = parseInt(
      prompt("Informe a quantidade de clientes que usam diesel")
    );
    diesel = resDiesel;
    document.writeln(`Diesel: ${diesel}`);
    preferedFuel();
  } else if (input == 4) {
    confirm("Deseja encerrar?");
    document.writeln("<hr>FIM");
  } else if (input == 0 || input > 4 || input != Number) {
    alert("Erro, escolha um número de 1 a 4 por favor");
    preferedFuel();
  }
} */